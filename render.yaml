# Render.com deployment configuration
# This file helps Render.com understand how to deploy your FastAPI application

# Python version
python_version: 3.11

# Build command
build_command: |
  pip install --upgrade pip
  pip install --no-cache-dir -r requirements.txt
  mkdir -p uploads logs

# Start command
start_command: python main.py

# Environment variables (set these in Render.com dashboard)
# DATABASE_URL=postgresql://prontivus_rh0l_user:eKdELoiPkpuvqiuD84ao7yfkltPy7oev@dpg-d39ab7fdiees7387nihg-a.oregon-postgres.render.com/prontivus_rh0l
# ENVIRONMENT=production
# USE_SQLITE=false
# USE_DATABASE=true
# ALLOWED_ORIGINS=["https://prontivus-frontend.vercel.app"]
# SECRET_KEY=your-production-secret-key-change-this
